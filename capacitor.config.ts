import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fantasyworldvisualizer.app',
  appName: 'Fantasy World Visualizer',
  webDir: 'dist'
};

export default config;
